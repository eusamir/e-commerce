//next automaticamente mostra o loading.tsx até PAGE.TSX renderizar

//herda também o layout

//é o root loading compartilhado para todas as paginas

export default function RootLoading(){
  return(
    <p>Carregando...</p>
  )
}